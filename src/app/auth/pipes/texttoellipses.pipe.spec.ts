import { TexttoellipsesPipe } from './texttoellipses.pipe';

describe('TexttoellipsesPipe', () => {
  it('create an instance', () => {
    const pipe = new TexttoellipsesPipe();
    expect(pipe).toBeTruthy();
  });
});
