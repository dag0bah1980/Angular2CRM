import { RemoveInvalidRendererDirective } from './remove-invalid-renderer.directive';

describe('RemoveInvalidRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveInvalidRendererDirective();
    expect(directive).toBeTruthy();
  });
});
