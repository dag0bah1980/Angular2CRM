import { AuthRendererDirective } from './auth-renderer.directive';

describe('AuthRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthRendererDirective();
    expect(directive).toBeTruthy();
  });
});
